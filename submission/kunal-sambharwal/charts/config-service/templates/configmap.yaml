apiVersion: v1
kind: ConfigMap

metadata:
  name: {{ include "config-service.fullname" . }}-config
  namespace: {{ .Values.namespace }}

data:
  DB_HOST: "{{ include "config-service.fullname" . }}-postgres"
  DB_PORT: "{{ .Values.postgres.port }}"
  DB_NAME: "{{ .Values.postgres.database }}"