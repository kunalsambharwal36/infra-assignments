apiVersion: v1
kind: ConfigMap

metadata:
  name: {{ include "config-service.fullname" . }}-db-init
  namespace: {{ .Values.namespace }}

data:
  001_create_configs.sql: |
{{ .Files.Get "migrations/001_create_configs.sql" | indent 4 }}